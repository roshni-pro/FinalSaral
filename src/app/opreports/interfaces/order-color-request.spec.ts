import { OrderColorRequest } from './order-color-request';

describe('OrderColorRequest', () => {
  it('should create an instance', () => {
    expect(new OrderColorRequest()).toBeTruthy();
  });
});
